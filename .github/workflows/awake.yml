name: Keep Space Awake
on:
  schedule:
    - cron: '0 12 * * *'
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
       - run: curl -I https://lohithmahesh-market-agn.hf.space/
